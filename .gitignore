# Local runtime data
runtime/

# Python cache
__pycache__/
*.pyc

# Local editor files
.vscode/

# Cloudflare / build output
.wrangler/
node_modules/

# Keep the actual frontend bundle separate from the local Python runtime files.
# The FFmpeg vendor directory is required for local processing and must remain available.
