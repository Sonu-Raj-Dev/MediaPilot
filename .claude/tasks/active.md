# Active

In progress, one owner each. Manager moves items here from `backlog.md` and
into `completed.md` when done and verified (tests + build green).

Format: `- [RISK] Title — owner agent — status note`

- [LOW] Track the 3 untracked test scripts in git — owner: developer —
  implemented, tested, built (branch `task/track-test-scripts`, commit
  `3ff00e5`), approval record `2026-09-15-track-test-scripts` filed
  pending. Also awaiting merge: the already-**approved**
  `2026-09-15-mark-dead-video-app-js` (commit `7b19434`) — this run's
  attempt to fast-forward `main` to it was blocked by this session's own
  Bash-tool "Merge Without Review" guardrail, not by any project rule; see
  today's report for detail.
